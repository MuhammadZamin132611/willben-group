import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'willben',
  webDir: 'www'
};

export default config;


// "outputPath": "dist/willben-group",
